# Copy to .env and fill in. Never commit the real .env.

# Developer Portal -> your application -> Bot -> Reset Token
DISCORD_TOKEN=

# Shown as a button on /help. Leave empty to hide the button.
DONATION_URL=

# Optional. The bot keeps everything in SQLite and does not need Supabase.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
